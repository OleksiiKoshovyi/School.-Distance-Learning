@model IEnumerable<School._Distance_Learning.Models.Homeworks>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PassDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Homework)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TeacherSubjectGroup)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PassDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Homework)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TeacherSubjectGroup.TeacherSubjectGroupName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.HomeworkId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.HomeworkId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.HomeworkId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
